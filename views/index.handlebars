<h1>Hungry? Let's eat some burgers!</h1>

<h2>Burgers ready to be devoured</h2>

<ul>
  {{#each burgers}}
    {{#unless sleepy}}
      {{> cats/cat-block sleep=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers devoured!</h2>

<ul>
  {{#each burgers}}
    {{#if sleepy}}
      {{> cats/cat-block sleep=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>First, add a burger to the list</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Cat Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="sleepy">Devour this burger now?</label><br>
    <input type="radio" name="sleepy" value="1" checked> Burger's gone down your tummy!<br>
    <input type="radio" name="sleepy" value="0"> Devoured
  </div>

  <button type="submit">Add burger</button>
</form>